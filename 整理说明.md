# 整理说明（仓库是如何从竞赛提交包整理出来的）

> 源提交包：`A+ak9HzkqIZG/`（第二十四届 SuperMap 杯参赛作品提交包）
> 整理原则：**不编造、不改算法**——所有数值、代码逻辑、文件内容均来自提交包实际文件；整理仅限目录重组、路径配置化、注释补充与文档撰写。

## 一、做了哪些改动

### 1. 目录重组（原结构 → 新结构）

| 提交包位置 | 仓库位置 | 说明 |
| --- | --- | --- |
| `3工程文件/Ch01_数据底座/_run_hillshade.py` | `src/ch01_data_foundation/run_hillshade.py` | 去掉下划线前缀 |
| `3工程文件/Ch02_光照分类/说明.txt` | `src/ch02_illumination/README.md` | 无代码章节，说明转为 README |
| `3工程文件/Ch03_水冰分布/*.py`（4 个） | `src/ch03_water_ice/` | 原名保留 |
| `3工程文件/Ch03_水冰分布/wang2025_code/` | （已移出公开仓库，本地另存） | 训练代码与预训练模型为 Wang 2025 论文成果，无转载授权 |
| `3工程文件/Ch04_安全势场/*.py`（8 个） | `src/ch04_safety/` | `欧式距离.py` 改名 `euclidean_distance.py`（ASCII 文件名利于跨平台） |
| `3工程文件/Ch05_AHP选址/蒙特卡洛1000次敏感性分析.py` | `src/ch05_ahp_site/monte_carlo_sensitivity.py` | 中文文件名改英文 |
| `3工程文件/Ch06_路径规划/*.py`（5 个） | `src/ch06_path_planning/` | 原名保留 |
| `3工程文件/Ch08_数字孪生/体元可视化平台.py` | `src/ch08_digital_twin/voxel_viz_platform.py` | 中文文件名改英文 |
| `3工程文件/Ch08_数字孪生/ThreeJS_scene/` | `src/ch08_digital_twin/ThreeJS_scene/` | 仅保留 index.html 实际引用的 7 个数据/纹理文件（见"三、未入库文件"） |
| `3工程文件/Ch04_安全势场/align_to_standard_grid.py`、`verify_crs_alignment.py` | `src/utils/` | 功能为跨章节铁基准工具，归入 utils |
| `2成果数据/GPA.smwu`、`GPA模型.xml` | `gpa_model/` | 均小于 1 MB，直接入库 |
| `2成果数据/统计分析结果/` | `results/stats/` + `results/charts/` | 图片归 charts、csv/txt 归 stats |
| `4成果展示/成果图片/`（10 张） | `results/maps/` | 全部入库 |
| `4成果展示/统计图片/`（8 张） | `results/charts/` | 全部入库 |
| `3工程文件/requirements.txt` | `requirements.txt` | 原样复制 |
| `月球南极选址方法论_终极合并版.txt`（98 KB） | `docs/methodology.md` | 浓缩重写为 Markdown（保留全部参数/公式/验证结论，删去内部协作过程性内容） |
| `5作品文档/5.1作品介绍.docx` 等 | `README.md` / `docs/` | 提取项目概述、结果与结论 |
| `7加分项说明/7.1地理处理自动化模型说明文档.docx` | `gpa_model/README.md` | 提取模型架构/50 节点/公式台账/部署方式（节点清单另行从 `GPA模型.xml` 逐节点解析核对） |

### 2. 代码整理（算法逻辑零改动）

对每个 Python 脚本只做了三类改动，全部可从文件头注释追溯：

1. **加文件头注释**：章节、功能一句话、来源原文件名；
2. **绝对路径 → 环境变量可覆盖**：如 `LUNAR_DATA_DIR`、`LUNAR_PROJECT_ROOT`、`LUNAR_UDBX`、`LUNAR_FACTOR_DIR`、`LUNAR_OUTPUT_DIR`、`LUNAR_SLOPE_TIF`、`LUNAR_SCAN_DIRS` 等，默认值保留原开发机路径以便对照；
3. **极少量语法级修复**：`energy_simulation.py` 整理时的引号笔误（已修复并全部通过 `py_compile`）；`plot_energy_curve_svg.py` docstring 中 Windows 路径反斜杠转义警告（改为正斜杠，不涉逻辑）。

全部 22 个脚本已通过 `python -m py_compile` 语法校验。

### 3. 新撰写的文档（提交包中不存在，为本仓库新写）

`README.md`、`docs/methodology.md`、`docs/results.md`、`docs/quickstart.md`、`data/README.md`、`gpa_model/README.md`、`src/ch02_illumination/README.md`、`src/ch08_digital_twin/README.md`、`examples/run_pipeline.py`、`LICENSE`、`.gitignore`、本文件。

## 二、因太大/不宜入库而未放入仓库的文件

| 文件 | 大小 | 原因与获取方式 |
| --- | --- | --- |
| `1原始数据/` 全部原始数据（LOLA DEM、CE-2 DEM、AVGVISIB、Diviner、LEND、Robbins、Wang 冰点 xlsx 等） | 数 GB | 获取方式与预处理规范见 `data/README.md` |
| `2成果数据/数据源/*.udbx`（7 个） | 合计约 100 MB | SuperMap 文件型数据库；GPA 模型运行时按 `gpa_model/README.md` 重连本地数据源 |
| `2成果数据/数据说明.xlsx` | 小 | 内容已转录进 `data/README.md` 与 `docs/methodology.md`（xlsx 二进制格式不适合版本管理） |
| `5作品文档/*.docx`、`6演示讲解/6.2演示文稿.pptx` | 数十 MB | 内容已提取进仓库文档；原文件留提交包 |
| `6演示讲解/6.1演示录像.mp4` | 大 | 视频不入库 |
| `ThreeJS_scene/dem_outer_ring.json` | 11 MB | 未被 index.html 引用（外围过渡地形升级方案） |
| `ThreeJS_scene/dem_outer_ring_1km.json` | 2.7 MB | 同上 |
| `ThreeJS_scene/lunar_south_dem_merged.tif` | 6 MB | 浏览器端不直接加载 tif（升级方案数据） |
| `ThreeJS_scene/lunar_south_dem_transition.tif`、`dem_outer_texture.png` | <1 MB | 同上（未引用） |
| `_归档/`（参数修正备份等） | — | 内部过程文件，不入库 |
| `4成果展示/过程图片/`（GPA 过程截图 8 张） | 小 | GPA 概览已由 `10_选址流程ModelBuilder概览.png` 代表，过程截图未重复入库 |

## 三、口径说明（整理时发现的数值差异）

提交包内部有两处口径差异，整理时采用**实测结果文件**口径并在文中注明：

1. **ECSA 诊断指标**：方法论 txt 记 CV=37.00% / r=0.2978 / ρ=0.4590（早期整幅数据）；
   实测报告 `step0_ecsa_diagnostic_report.txt` 与作品介绍均为 **CV=82.8% / r=0.3050 / ρ=0.3675**（研究区裁剪后）。
   仓库统一采用后者，并保留"两项指标均满足路径 B"的一致结论。
2. **适宜性分级**：方法论 §5.5.2 写"三级"，作品介绍与 GPA 模型（reclass 节点断点
   0.332/0.467/0.542/0.572/0.652）为**五级**（Ⅰ级最优）。仓库按最终提交口径"Jenks 五级、
   Ⅰ级站址"表述。

## 四、Git 使用提示

```bash
cd Lunar-South-Pole-Site-Selection
git init
git add .
git commit -m "init: 超图杯参赛作品开源整理（月球南极选址）"
# GitHub 上新建空仓库后：
git remote add origin https://github.com/<your-username>/Lunar-South-Pole-Site-Selection.git
git branch -M main
git push -u origin main
```

建议同时启用 GitHub Pages 把 `src/ch08_digital_twin/ThreeJS_scene/` 托管为在线演示
（该目录本身即静态站点，无需构建）。
